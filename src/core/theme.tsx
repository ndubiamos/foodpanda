import { DefaultTheme } from "react-native-paper";

export const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: "#d70f64",
    secondary: "#414757",
    error: "#e771a3",
    success: "#d70465"
  }
};
